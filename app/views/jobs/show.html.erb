<h3><%= @job.title %></h3>
<p><%= @job.description %></p>
<% if current_user.has_role? :admin %>
  <%= link_to "Edit", edit_job_path(@job) %>
  <%= button_to "Destroy", job_path(@job),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %>
<% end %>

